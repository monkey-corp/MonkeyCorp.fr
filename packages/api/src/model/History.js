export default class History
{
    constructor({key, title, paragraphs, images} = {}) {
        this.key = key
        this.title = title
        this.paragraphs = paragraphs
        this.images = images
    }
}